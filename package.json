{
  "name": "scentmap",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.js && node scripts/generate-og-images.js && node scripts/build-sitemap.js",
    "sitemap": "node scripts/build-sitemap.js",
    "og-images": "node scripts/generate-og-images.js",
    "test": "node test/fragrance-api.test.js"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@vercel/og": "^0.6.0",
    "satori": "^0.29.0"
  }
}
